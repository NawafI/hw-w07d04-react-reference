import React, { Component } from 'react';

class LyfeCycleInfo extends Component {
    render(){
        return(
<div>

<h1 className="title">The Component Lifecycle</h1>
            <h4 className ="Content">
            Each component has several “lifecycle methods” that you can override to run code at particular times in the process. You can use this lifecycle diagram as a cheat sheet. In the list below, commonly used lifecycle methods are marked as bold. The rest of them exist for relatively rare use cases.</h4>
</div>
            
        )
    }
}








export default LyfeCycleInfo;